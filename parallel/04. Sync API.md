* решение задачи обедующих философов: мьютексы на каждый ресурс (вилка), один мьютекс (только один философ (поток) может обедать/пытаться захватить вилки - без конкуренции)
* полезная штука - через сигналы можно своеобразно переопределять действия; в частности можно снимать lock с мьютексами при падение потока (?)
* volatile - чтобы  компилятор не оптимизировал то, что на первый взгляд  может не изменяться, но на деле меняется где-то неявно за пределами данного контекста (функции); при этом он не ставит неявно барьеры памяти

1. серия экспериментов:
	* несколько потоков; каждый изменяет глобальную переменную - много +(конкурентно, но без всяких блокировок для взаимного исключения); понятно, что были инкременты (в параллельных потоках), которые вместе довали один инкремент
	* если убрать volatile - получится, что операция инкремент - это load -> + -> store; во-первых с оптимизируется в один инкремент, во-вторых, проблема осталось, потому что в разные потоки загрузится (прочитается) одно и то же значение, вместо последовательности
2. с мьютексом:
	* время работы раз увеличилось раз в 100
3. со спин-локом:
	* примерно тоже самое, что и мьютекс - вообще немного медленнее
	* примечание: обычно ОС не сразу отправляет в пространство ядра информацию и усыпляет поток - часто сначала происходит спин-лок (**futex**)
4. атомарные операции:
	* сильно быстрее мьютексов/спин-локов, но сильно медленнее, чем без синхронизации

*сузить число мест с синхронизацией - делать по максимому действий до того, как делать действие с синхронизацией и делать одно на группу действий (сначала все считать, а потом изменять разделяемое значение)* - **сильное улучшение (близкие к варианту без синхронизации)** 

**memory order в atomic**: разрешение переупорядочиваний операций 

### OpenMP
* `#pragma omp parallel` - независимые действия в блоке после распараллелятся
* можно прописывать комбинирование результатов (reduction)


Примитивы:
* *RWlock* (приоритет Писателя/Читателя - стратегии)
* *Barier* (ожидание выполнения n потоков; до момента пока все доработают,  завершившиеся до точки барьера потоки блокируются)
* *CondVar* (обычно в тандеме мьютекса - добавляется проверка условия; при провале условия - сон, при этом другой поток явно будит текущий при освобождение ресурса)
* 


